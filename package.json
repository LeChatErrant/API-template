{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "pre-commit": "npm run lint:ci && npm run db:format",
    "build": "tsc",
    "start": "npm run db:deploy && node dist",
    "test": "MODE=test && exit 0",
    "integration": "MODE=test && npm run db:deploy && jest --passWithNoTests --coverage --runInBand --forceExit",
    "integration:watch": "MODE=test && npm run db:deploy && jest --passWithNoTests --coverage --runInBand --forceExit --watch",
    "dev:db": "docker run --name db -e POSTGRES_PASSWORD=${DB_PASS} -e POSTGRES_USER=${DB_USER} -p ${DB_PORT}:${DB_PORT} -d postgres:alpine",
    "dev:redis": "docker run --name redis -e REDIS_PASSWORD=${REDIS_PASS} -p ${REDIS_PORT}:${REDIS_PORT} -d bitnami/redis:latest",
    "dev": "npm run db:deploy && concurrently --kill-others 'prisma generate --watch' 'ts-node-dev --respawn --cls --exit-child src'",
    "prisma:generate": "prisma generate",
    "db:migrate:help": "prisma migrate --help",
    "db:migrate": "prisma migrate dev --preview-feature",
    "db:migrate:draft": "prisma migrate dev --create-only --preview-feature",
    "db:reset": "prisma migrate reset --preview-feature",
    "db:deploy": "prisma migrate deploy --preview-feature",
    "db:deploy:status": "prisma migrate status --preview-feature",
    "db:format": "prisma format",
    "db:studio": "prisma studio --port ${STUDIO_PORT}",
    "lint": "eslint './src/**/*.{ts,tsx}' --fix",
    "lint:ci": "eslint './src/**/*.{ts,tsx}'",
    "postinstall": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bcrypt": "^3.0.0",
    "@types/connect-redis": "0.0.16",
    "@types/express": "^4.17.11",
    "@types/express-session": "^1.17.3",
    "@types/http-errors": "^1.8.0",
    "@types/jest": "^26.0.20",
    "@types/morgan": "^1.9.2",
    "@types/node": "^14.14.31",
    "@types/redis": "^2.8.28",
    "@types/supertest": "^2.0.10",
    "@typescript-eslint/eslint-plugin": "^2.34.0",
    "@typescript-eslint/parser": "^2.33.0",
    "concurrently": "^6.0.0",
    "eslint": "^7.21.0",
    "eslint-config-airbnb-typescript": "^7.2.1",
    "eslint-plugin-import": "^2.22.1",
    "husky": "^5.1.1",
    "jest": "^26.6.3",
    "supertest": "^6.1.3",
    "ts-jest": "^26.5.2",
    "ts-node-dev": "^1.1.6"
  },
  "dependencies": {
    "@prisma/client": "^2.17.0",
    "bcrypt": "^5.0.1",
    "class-transformer": "^0.3.1",
    "class-validator": "^0.12.2",
    "connect-redis": "^5.1.0",
    "env-var": "^7.0.0",
    "express": "^4.17.1",
    "express-async-handler": "^1.1.4",
    "express-session": "^1.17.1",
    "http-errors": "^1.8.0",
    "http-status-codes": "^2.1.4",
    "morgan": "^1.10.0",
    "prisma": "^2.17.0",
    "redis": "^3.0.2",
    "reflect-metadata": "^0.1.13",
    "typescript": "^4.2.2",
    "winston": "^3.3.3"
  }
}
