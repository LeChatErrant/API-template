/**
 * Either a type or a nullish value
 */
export type Maybe<T> = T | null | undefined;

/**
 * Type definition that will be written later
 *
 * Useful when mapping external API objects to indicate a key that have not been already typed
 */
export type Todo = unknown;
